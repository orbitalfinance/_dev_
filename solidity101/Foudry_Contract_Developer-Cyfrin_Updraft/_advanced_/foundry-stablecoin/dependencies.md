# Dependencies

This project vendors dependencies inside `lib/` because the repository is managed as a monorepo with a single parent `.git`.

## Installed dependencies

```bash
forge install smartcontractkit/chainlink-evm@cre-v0.4.1 --no-git
forge install OpenZeppelin/openzeppelin-contracts@v5.6.1 --no-git
